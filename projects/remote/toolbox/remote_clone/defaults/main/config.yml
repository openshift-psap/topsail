# Auto-generated file, do not edit manually ...
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: Remote.clone

# Parameters
# the URL of the repo to clone
# Mandatory value
remote_clone_repo_url:

# the directory where the repo should be cloned
# Mandatory value
remote_clone_dest:

# the git version to clone
remote_clone_version: null

# the git ref to clone. Can't be set with version
remote_clone_refspec: null

# force the git clone
remote_clone_force: false

# Default Ansible variables
# Default value for ansible_os_family to ensure role remains standalone
ansible_os_family: Linux
