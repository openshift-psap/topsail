---
- name: Ensure that the parent directory exists
  ansible.builtin.file:
    state: directory
    path: "{{ container_bench_copy_file_to_remote_dest | dirname }}"
    mode: '0755'

- name: Copy a file to the remote host
  ansible.builtin.copy:
    src: "{{ container_bench_copy_file_to_remote_source }}"
    dest: "{{ container_bench_copy_file_to_remote_dest }}"
