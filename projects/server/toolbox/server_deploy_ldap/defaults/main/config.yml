# Auto-generated file, do not edit manually ...
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: Server.deploy_ldap

# Parameters
# Name of the LDAP identity provider.
# Mandatory value
server_deploy_ldap_idp_name:

# Prefix for the creation of the users (suffix is 0..username_count)
# Mandatory value
server_deploy_ldap_username_prefix:

# Number of users to create.
# Mandatory value
# Type: Int
server_deploy_ldap_username_count:

# Path of a file containing the properties of LDAP secrets.
# Mandatory value
server_deploy_ldap_secret_properties_file:

# If true, use `ocm create idp` to deploy the LDAP identity provider.
server_deploy_ldap_use_ocm: false

# If true, use `rosa create idp` to deploy the LDAP identity provider.
server_deploy_ldap_use_rosa: false

# Cluster to use when using OCM or ROSA.
server_deploy_ldap_cluster_name: null

# If True, waits for the first user (0) to be able to login into the cluster.
server_deploy_ldap_wait: false

# Default Ansible variables
# Default value for ansible_os_family to ensure role remains standalone
ansible_os_family: Linux

# Constants
# Name of the admin user
# Defined as a constant in Server.deploy_ldap
server_deploy_ldap_admin_user: admin
