---
# The instance type to use for the machineset being scaled
machineset_instance_type: "g4dn.xlarge"

machineset_name:
machineset_taint:

cluster_ensure_machineset_base_machineset:

cluster_ensure_machineset_spot: false

cluster_ensure_machineset_disk_size: nil
