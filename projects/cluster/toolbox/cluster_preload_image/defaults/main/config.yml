# Auto-generated file, do not edit manually ...
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: Cluster.preload_image

# Parameters
# Name to give to the DaemonSet used for preloading the image.
# Mandatory value
cluster_preload_image_name:

# Container image to preload on the nodes.
# Mandatory value
cluster_preload_image_image:

# Namespace in which the DaemonSet will be created.
cluster_preload_image_namespace: default

# NodeSelector key to apply to the DaemonSet.
cluster_preload_image_node_selector_key:

# NodeSelector value to apply to the DaemonSet.
cluster_preload_image_node_selector_value:

# Pod toleration to apply to the DaemonSet.
cluster_preload_image_pod_toleration_key:

# Pod toleration to apply to the DaemonSet.
cluster_preload_image_pod_toleration_effect:

# Default Ansible variables
# Default value for ansible_os_family to ensure role remains standalone
ansible_os_family: Linux
