# Auto-generated file, do not edit manually ...
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: Cluster.set_project_annotation

# Parameters
# The annotation key
# Mandatory value
cluster_set_project_annotation_key:

# The annotation value. If value is omited, the annotation is removed.
cluster_set_project_annotation_value: null

# The project to annotate. Must be set unless --all is passed.
cluster_set_project_annotation_project: null

# If set, the annotation will be set for any new project.
cluster_set_project_annotation_all: false

# Default Ansible variables
# Default value for ansible_os_family to ensure role remains standalone
ansible_os_family: Linux
