apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: isvc
spec:
  predictor:
    minReplicas: 1
    maxReplicas: 1
    model:
      storageUri: s3://psap-1474-blog-models/ibm-granite/granite-3.1-8b-instruct/ 
      args:
      - --tensor-parallel-size=1
      - --enable-chunked-prefill
      - --gpu-memory-utilization=0.95
      resources:
        requests:
          cpu: "2"
          memory: "16Gi"
          nvidia.com/gpu: "1"
        limits:
          nvidia.com/gpu: "1"
