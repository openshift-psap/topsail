# Auto-generated file, do not edit manually ...
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: Kserve.validate_model

# Parameters
# a list of names of the inference service to validate
# Mandatory value
kserve_validate_model_inference_service_names:

# number of query to perform
# Mandatory value
kserve_validate_model_query_count:

# name of the runtime used (standalone-tgis or vllm)
# Mandatory value
kserve_validate_model_runtime:

# the model-id to pass to the inference service
kserve_validate_model_model_id: not-used

# the namespace in which the Serving stack was deployed. If empty, use the current project.
kserve_validate_model_namespace:

# if True, do not try to configure anything related to Serverless. Works only in-cluster at the moment.
kserve_validate_model_raw_deployment: false

# the gRPC method to call #TODO remove?
kserve_validate_model_method: null

# if not empty, the proto file to pass to grpcurl
kserve_validate_model_proto: null
