# Auto-generated file, do not edit manually ...
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: Pipelines.capture_state

# Parameters
# the name of the application
pipelines_capture_state_dsp_application_name:

# the namespace in which the application was deployed
pipelines_capture_state_namespace:

# identifier of the user to capture
pipelines_capture_state_user_id:

# whether to capture extra descriptions and YAML's
pipelines_capture_state_capture_extra_artifacts: true
