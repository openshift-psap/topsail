# Auto-generated file, do not edit manually ...
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: Notebooks.ods_ci_scale_test

# Parameters
# Namespace in which the scale test should be deployed.
# Mandatory value
notebooks_ods_ci_scale_test_namespace:

# Name of the identity provider to use.
# Mandatory value
notebooks_ods_ci_scale_test_idp_name:

# Prefix of the usernames to use to run the scale test.
# Mandatory value
notebooks_ods_ci_scale_test_username_prefix:

# Number of users to run in parallel.
# Mandatory value
# Type: Int
notebooks_ods_ci_scale_test_user_count:

# Path of a file containing the properties of LDAP secrets. (See 'deploy_ldap' command)
# Mandatory value
notebooks_ods_ci_scale_test_secret_properties_file:

# URL from which the notebook will be downloaded.
# Mandatory value
notebooks_ods_ci_scale_test_notebook_url:

# Namespace where the Minio server is located.
# Mandatory value
notebooks_ods_ci_scale_test_minio_namespace:

# Name of the bucket in the Minio server.
# Mandatory value
notebooks_ods_ci_scale_test_minio_bucket_name:

# Offset to add to the user index to compute the user name.
# Type: Int
notebooks_ods_ci_scale_test_user_index_offset: 0

# Path of the system-under-test cluster's Kubeconfig. If provided, the RHODS endpoints will be looked up in this cluster.
notebooks_ods_ci_scale_test_sut_cluster_kubeconfig:

# - 'all' - 'no-screenshot' - 'no-screenshot-except-zero' - 'no-screenshot-except-failed' - 'no-screenshot-except-failed-and-zero' - 'none'
notebooks_ods_ci_scale_test_artifacts_collected: all

# Delay to sleep between users
notebooks_ods_ci_scale_test_user_sleep_factor: 1.0

# Number of users to launch at the same time.
# Type: Int
notebooks_ods_ci_scale_test_user_batch_size: 1

# Imagestream tag of the ODS-CI container image.
notebooks_ods_ci_scale_test_ods_ci_istag:

# Tags to exclude in the ODS-CI test case.
notebooks_ods_ci_scale_test_ods_ci_exclude_tags: None

# Robot test case name.
notebooks_ods_ci_scale_test_ods_ci_test_case: notebook_dsg_test.robot

# Imagestream tag of the artifacts exporter side-car container image.
notebooks_ods_ci_scale_test_artifacts_exporter_istag:

# Notebook image name.
notebooks_ods_ci_scale_test_notebook_image_name: s2i-generic-data-science-notebook

# Notebook size.
notebooks_ods_ci_scale_test_notebook_size_name: Small

# Benchmark script file name to execute in the notebook.
notebooks_ods_ci_scale_test_notebook_benchmark_name: pyperf_bm_go.py

# Number of the benchmarks executions per repeat.
notebooks_ods_ci_scale_test_notebook_benchmark_number: 20

# Number of the benchmark repeats to execute.
notebooks_ods_ci_scale_test_notebook_benchmark_repeat: 2

# Hostname and port of the Redis server for StateSignal synchronization (for the synchronization of the beginning of the user simulation)
notebooks_ods_ci_scale_test_state_signal_redis_server:

# Toleration key to use for the test Pods.
notebooks_ods_ci_scale_test_toleration_key:

# If True, captures the Prometheus DB of the systems.
# Type: Bool
notebooks_ods_ci_scale_test_capture_prom_db: true

# If False, keep the user notebooks running at the end of the test.
# Type: Bool
notebooks_ods_ci_scale_test_stop_notebooks_on_exit: true

# If True, only create the notebooks, but don't start them. This will overwrite the value of 'ods_ci_exclude_tags'.
# Type: Bool
notebooks_ods_ci_scale_test_only_create_notebooks: false

# If True, consider that the driver Pods are running on Spot instances and can disappear at any time.
# Type: Bool
notebooks_ods_ci_scale_test_driver_running_on_spot: false
