# Auto-generated file, do not edit manually ... 
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: RHODS.notebook_api_scale_test

# Namespace where the test will run
# Mandatory value
rhods_notebook_api_scale_test_namespace:

# Name of the identity provider to use.
# Mandatory value
rhods_notebook_api_scale_test_idp_name:

# Path of a file containing the properties of LDAP secrets. (See 'deploy_ldap' command).
# Mandatory value
rhods_notebook_api_scale_test_secret_properties_file:

# Prefix of the RHODS users.
# Mandatory value
rhods_notebook_api_scale_test_username_prefix:

# Test to perform.
# Mandatory value
rhods_notebook_api_scale_test_test_name:

# Number of users to run in parallel.
# Mandatory value
# Type: Int
rhods_notebook_api_scale_test_user_count:

# Test run time (eg, 300s, 20m, 3h, 1h30m, etc.)
rhods_notebook_api_scale_test_run_time: 1m

# Rate to spawn users at (users per second)
rhods_notebook_api_scale_test_spawn_rate: '1'

# Path of the system-under-test cluster's Kubeconfig. If provided, the RHODS endpoints will be looked up in this cluster.
rhods_notebook_api_scale_test_sut_cluster_kubeconfig:

# Name of the RHODS image to use when launching the notebooks.
rhods_notebook_api_scale_test_notebook_image_name: s2i-generic-data-science-notebook

# Image stream tag.
rhods_notebook_api_scale_test_api_scale_test_istag: ods-ci:api-scale-test

# Imagestream tag of the artifacts exporter side-car container.
rhods_notebook_api_scale_test_artifacts_exporter_istag: ods-ci:artifacts-exporter

# Toleration key to use for the test Pods.
rhods_notebook_api_scale_test_toleration_key:

