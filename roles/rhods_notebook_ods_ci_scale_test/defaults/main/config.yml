# Auto-generated file, do not edit manually ... 
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: RHODS.notebook_ods_ci_scale_test

# Namespace in which the scale test should be deployed.
# Mandatory value
rhods_notebook_ods_ci_scale_test_namespace:

# Name of the identity provider to use.
# Mandatory value
rhods_notebook_ods_ci_scale_test_idp_name:

# Prefix of the usernames to use to run the scale test.
# Mandatory value
rhods_notebook_ods_ci_scale_test_username_prefix:

# Number of users to run in parallel.
# Mandatory value
# Type: Int
rhods_notebook_ods_ci_scale_test_user_count:

# Path of a file containing the properties of LDAP secrets. (See 'deploy_ldap' command)
# Mandatory value
rhods_notebook_ods_ci_scale_test_secret_properties_file:

# URL from which the notebook will be downloaded.
# Mandatory value
rhods_notebook_ods_ci_scale_test_notebook_url:

# Offset to add to the user index to compute the user name.
# Type: Int
rhods_notebook_ods_ci_scale_test_user_index_offset: 0

# Path of the system-under-test cluster's Kubeconfig. If provided, the RHODS endpoints will be looked up in this cluster.
rhods_notebook_ods_ci_scale_test_sut_cluster_kubeconfig:

# - 'all' - 'no-screenshot' - 'no-screenshot-except-zero' - 'no-screenshot-except-failed' - 'no-screenshot-except-failed-and-zero' - 'none'
rhods_notebook_ods_ci_scale_test_artifacts_collected: all

# Delay to sleep between users
rhods_notebook_ods_ci_scale_test_user_sleep_factor: 1.0

# Imagestream tag of the ODS-CI container image.
rhods_notebook_ods_ci_scale_test_ods_ci_istag:

# Tags to exclude in the ODS-CI test case.
rhods_notebook_ods_ci_scale_test_ods_ci_exclude_tags: None

# Missing documentation for ods_ci_test_case
rhods_notebook_ods_ci_scale_test_ods_ci_test_case: notebook_ux_e2e_test.robot

# Imagestream tag of the artifacts exporter side-car container image.
rhods_notebook_ods_ci_scale_test_artifacts_exporter_istag:

# Missing documentation for notebook_image_name
rhods_notebook_ods_ci_scale_test_notebook_image_name: s2i-generic-data-science-notebook

# Missing documentation for notebook_size_name
rhods_notebook_ods_ci_scale_test_notebook_size_name: Small

# Missing documentation for notebook_benchmark_name
rhods_notebook_ods_ci_scale_test_notebook_benchmark_name: pyperf_bm_go.py

# Missing documentation for notebook_benchmark_number
rhods_notebook_ods_ci_scale_test_notebook_benchmark_number: 20

# Missing documentation for notebook_benchmark_repeat
rhods_notebook_ods_ci_scale_test_notebook_benchmark_repeat: 2

# Hostname and port of the Redis server for StateSignal synchronization (for the synchronization of the beginning of the user simulation)
rhods_notebook_ods_ci_scale_test_state_signal_redis_server:

# Toleration key to use for the test Pods.
rhods_notebook_ods_ci_scale_test_toleration_key:

