# Auto-generated file, do not edit manually ... 
# Toolbox generate command: repo generate_ansible_default_settings
# Source component: RHODS.notebook_locust_scale_test

# Namespace where the test will run
# Mandatory value
rhods_notebook_locust_scale_test_namespace:

# Name of the identity provider to use.
# Mandatory value
rhods_notebook_locust_scale_test_idp_name:

# Path of a file containing the properties of LDAP secrets. (See 'deploy_ldap' command).
# Mandatory value
rhods_notebook_locust_scale_test_secret_properties_file:

# Test to perform.
# Mandatory value
rhods_notebook_locust_scale_test_test_name:

# Prefix of the RHODS users.
# Mandatory value
rhods_notebook_locust_scale_test_username_prefix:

# Number of users to run in parallel.
# Mandatory value
# Type: Int
rhods_notebook_locust_scale_test_user_count:

# Offset to add to the user index to compute the user name.
# Mandatory value
# Type: Int
rhods_notebook_locust_scale_test_user_index_offset:

# Imagestream tag of the locust container.
# Mandatory value
rhods_notebook_locust_scale_test_locust_istag:

# Imagestream tag of the artifacts exporter side-car container.
# Mandatory value
rhods_notebook_locust_scale_test_artifacts_exporter_istag:

# Test run time (eg, 300s, 20m, 3h, 1h30m, etc.)
rhods_notebook_locust_scale_test_run_time: 1m

# Rate to spawn users at (users per second)
rhods_notebook_locust_scale_test_spawn_rate: '1'

# Path of the system-under-test cluster's Kubeconfig. If provided, the RHODS endpoints will be looked up in this cluster.
rhods_notebook_locust_scale_test_sut_cluster_kubeconfig:

# Name of the RHODS image to use when launching the notebooks.
rhods_notebook_locust_scale_test_notebook_image_name: s2i-generic-data-science-notebook

# Missing documentation for notebook_size_name
rhods_notebook_locust_scale_test_notebook_size_name: Small

# Toleration key to use for the test Pods.
rhods_notebook_locust_scale_test_toleration_key:

# Number of Locust processes to launch (one per Pod with 1cpu).
# Type: Int
rhods_notebook_locust_scale_test_cpu_count: 1

# Delay to sleep between users
# Type: Float
rhods_notebook_locust_scale_test_user_sleep_factor: 1.0

