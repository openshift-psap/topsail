# This file is used in `testing/notebooks/clusters.sh::create_clusters` when
# the `keep` flag is passed (so that the cluster don't get
# destroyed). A htpasswd identity provider is created with the PR
# author as username and this password, with admin privilges.

# This file is used in `cluster create_htpasswd_user` to set the
# password of the on-demand cluster.

# The file is sourced and must define `$password`.

password=...
