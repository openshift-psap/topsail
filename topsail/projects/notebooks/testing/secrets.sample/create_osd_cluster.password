# This is used in `osd_cluster.sh` to call:
# - ./run_toolbox.py cluster create_osd --secret-file=$THIS_FILE

# This file is sourced in `common.sh` to call:
# - oc login --password="$KUBEADMIN_PASS"

# AWS Account ID
AWS_ACCOUNT_ID=

# AWS access key & secret key
# These credentials are used to launch the OSD cluster.
# They must belong to an `osdCcsAdmin` account, as per OSD/OCM requirements.
AWS_ACCESS_KEY=
AWS_SECRET_KEY=

# The htpasswd password that will be set for the kubeadmin user of the OSD cluster
KUBEADMIN_PASS=
