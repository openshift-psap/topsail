---
apiVersion: v1
kind: Secret
metadata:
  name: openldap
  namespace: openldap
type: Opaque
data:
  adminuser: "$cluster_ldap_adminuser64"
  adminpassword: "$cluster_ldap_adminpassword64"
  passwords: "$cluster_ldap_passwords64"
  users: "$cluster_ldap_users64"
