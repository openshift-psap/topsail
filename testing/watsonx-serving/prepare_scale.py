import logging
import pathlib

from common import env, config, run

def prepare():
    """
    Prepares the cluster and the namespace for running the MCAD tests
    """

    logging.warning("Nothing to do to preapre the scale test")
