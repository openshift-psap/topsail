# OpenSSL configuration file for creating a CSR for a server certificate

# the name of your organization
ORGNAME = Red Hat Client

[ req ]
default_bits = 4096
default_md = sha256
prompt = no
encrypt_key = no
distinguished_name = dn

[ dn ]
C = US
O = $ORGNAME
